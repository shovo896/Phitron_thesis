# A2SNN Overleaf Journal Draft

Upload this folder or the generated ZIP file to Overleaf.

Main files:

- `main.tex`: IEEE-style journal draft with model architecture and hardware architecture diagrams.
- `references.bib`: BibTeX references.
- `budget_and_evaluation.tex`: Bangladesh marketplace-style budget and experimental planning file with BDT ranges.

Recommended Overleaf settings:

- Compiler: pdfLaTeX
- Main document: `main.tex`

Before submission:

- Replace author name, affiliation, and acknowledgment.
- Replace all placeholder result cells marked `--` with measured values.
- Add the exact FPGA/SNN paper you are comparing against to `references.bib`.
- Update budget numbers with invoice prices before final thesis submission.
- Recompile and check figure placement after adding measured result tables.
